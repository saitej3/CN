#include<stdio.h>
#include<unistd.h>
#include<stdlib.h>

int main()
{
	//FILE * f;
	//f=popen("third",0);
	//char buf[128];
	char buf[128];
	scanf("%s",buf);
	printf("%s",buf);
	//write(f,buf,128);
}
